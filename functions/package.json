{
  "name": "slow-fire-functions",
  "description": "Cloud Functions for SLOW FIRE SHOP — Stripe Checkout, OGP fetch, order webhook",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.0.0",
    "stripe": "^17.0.0",
    "node-fetch": "^2.7.0"
  },
  "private": true
}
